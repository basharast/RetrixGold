<Page
    x:Class="RetriX.UWP.Pages.AboutView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <Style x:Key="SectionTBStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="0,32,0,0"/>
        </Style>
    </Page.Resources>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ProgressBar IsIndeterminate="True" Height="8" x:Name="WeLoadingProgress" Canvas.ZIndex="10" VerticalAlignment="Top"></ProgressBar>
        <ProgressRing x:Name="WeLoadingProgressRing" Canvas.ZIndex="10" Width="50" IsEnabled="True" IsActive="True" Height="50" HorizontalAlignment="Center" VerticalAlignment="Center"></ProgressRing>
        <WebView HorizontalAlignment="Stretch" Loaded="WebView_Loaded" DOMContentLoaded="WebView_DOMContentLoaded" LoadCompleted="WebView_LoadCompleted" ContentLoading="WebView_ContentLoading" Source="ms-appx-web:///Assets/HTML/terms.html" VerticalAlignment="Stretch"></WebView>
    </Grid>
</Page>
